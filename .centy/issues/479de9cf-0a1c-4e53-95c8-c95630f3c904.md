---
displayNumber: 12
status: in-progress
priority: 2
createdAt: 2026-02-26T04:00:00.000000+00:00
updatedAt: 2026-02-26T01:41:19.424013+00:00
---

# feat: skeleton loading states

## Overview

The current page uses `opacity: 0` + CSS `animate-in` for reveal animations. There are no skeleton placeholders, so the page appears blank during SSR hydration on slow connections.

## Tasks

- [ ] Create a `PkgRowSkeleton` component that mirrors the `PkgRow` dimensions with a shimmer effect
- [ ] Create a `SectionSkeleton` that renders a section header + N skeleton rows
- [ ] Use `<Suspense>` boundaries around data-fetching sections so skeletons show immediately
- [ ] Ensure skeletons are accessible (use `aria-busy` / `aria-label`)

## Acceptance Criteria

- [ ] Skeleton UI is visible before data loads on a simulated slow (3G) connection
- [ ] No cumulative layout shift (CLS) when real content replaces skeletons
- [ ] Skeletons match the exact dimensions of the real rows
