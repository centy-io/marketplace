---
# This file is managed by Centy. Use the Centy CLI to modify it.
displayNumber: 25
status: in-progress
priority: 3
createdAt: 2026-02-26T01:54:22.573705+00:00
updatedAt: 2026-03-03T20:09:01.091878+00:00
---

# fix: comply with ddd/require-spec-file rule in config and mock files

## Overview

The `ddd/require-spec-file` rule is currently disabled for two file groups in `eslint.config.mjs`:

* `next.config.ts` and `src/instrumentation.ts` (framework entry points)
* `src/mocks/**` (MSW test infrastructure)

## Goal

Understand the rule’s intent and either add the required spec/test files, configure the rule to exclude framework files natively, or upstream a fix to `eslint-config-agent` so the override is not needed here.

## Tasks

* [ ] Review what `ddd/require-spec-file` enforces and whether framework files like `next.config.ts` should be excluded at the plugin level
* [ ] Add spec files for any files that genuinely need them (e.g., MSW handlers)
* [ ] If framework files should be globally excluded, open an issue or PR in `eslint-config-agent`
* [ ] Remove both `'ddd/require-spec-file': 'off'` overrides from `eslint.config.mjs`

## Acceptance Criteria

* [ ] `ddd/require-spec-file` runs without being suppressed in this repo
* [ ] ESLint passes without either override
